/**
 * Expenses Integration Tests
 */

const tests = [
  {
    name: 'Create expense → Upload receipt → Approve expense → Process reimbursement',
    phase: 'expenses',
    async setup(runner) {},
    async execute(runner) {
      // TODO: Implement test
      throw new Error('Test not yet implemented');
    },
    async teardown(runner) {}
  }
];

export default tests;
